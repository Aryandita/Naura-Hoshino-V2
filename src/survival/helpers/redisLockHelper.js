"use strict";

/**
 * @file redisLockHelper.js (Survival Proxy)
 * @description Proxy helper untuk distributed & in-memory locking dari src/utils/redisLockHelper.
 */

const redisLockHelper = require("../../utils/redisLockHelper");

module.exports = redisLockHelper;
